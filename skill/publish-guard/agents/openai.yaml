interface:
  display_name: "Publish Guard"
  short_description: "Pre-release audit for GitHub repos and ClawHub skills"
  default_prompt: "Use $publish-guard to audit the public surface before release. Check README, SKILL.md, release notes, and public metadata for leaks, broken first-run paths, and public-audience mismatch."

